services:
  - type: web
    name: sam-law-portal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /
    autoDeploy: true
    branch: main
    rootDir: .
